# راهنمای نصب نسخه‌ی تست اپلیکیشن «ماهم» روی آیفون

**سند راهنمای نصب — نسخه‌ی آزمایشی (Sideload)**

| | |
|---|---|
| نام اپلیکیشن | mahem_app |
| شناسه‌ی بسته (Bundle ID) | `com.mahem` |
| نسخه | ۱٫۰ (بیلد ۱) |
| فایل تحویلی | `mahem.ipa` |
| حجم | ۱۵۹ مگابایت |
| SHA-256 | `9d7883f84734c74028ab8e6531d1eaf801d7816e2ccdabec2810c126a7334ae4` |
| حداقل نسخه‌ی iOS | ۱۳٫۰ |
| معماری | arm64 |
| تاریخ تهیه | ۱۴۰۵/۰۵/۱۰ — ۱ اوت ۲۰۲۶ |

---

## ۱. مقدمه

این سند مراحل نصب نسخه‌ی آزمایشی اپلیکیشن «ماهم» را روی دستگاه iPhone شما توضیح می‌دهد.

از آنجا که این نسخه از طریق App Store یا TestFlight توزیع نمی‌شود، نصب آن نیازمند یک مرحله‌ی امضای مجدد (re-signing) با استفاده از Apple ID شخصی شماست. این فرایند روش رسمی و پشتیبانی‌شده‌ی اپل برای اجرای برنامه‌های در حال توسعه است و **نیازی به جیلبریک ندارد**.

لطفاً پیش از شروع، بخش **۵ (محدودیت‌های شناخته‌شده)** را مطالعه بفرمایید.

---

## ۲. پیش‌نیازها

برای انجام این فرایند به موارد زیر نیاز دارید:

| مورد | توضیح |
|---|---|
| رایانه‌ی Mac | برای اجرای ابزار امضا. نصب اولیه فقط از این طریق ممکن است. |
| کابل USB | برای اتصال آیفون به مک. **در نصب اولیه الزامی است.** |
| Apple ID | حساب رایگان کفایت می‌کند؛ نیازی به حساب توسعه‌دهنده‌ی ۹۹ دلاری نیست. |
| فایل `mahem.ipa` | فایلی که برای شما ارسال شده است. |

---

## ۳. آماده‌سازی حساب Apple ID

اگر روی حساب Apple ID شما **ورود دو مرحله‌ای (Two-Factor Authentication)** فعال است — که برای اکثر حساب‌ها به‌صورت پیش‌فرض فعال است — باید یک رمز اختصاصی برای این ابزار بسازید:

1. به نشانی [appleid.apple.com](https://appleid.apple.com) مراجعه و وارد حساب خود شوید.
2. وارد بخش **Sign-In and Security** شوید.
3. گزینه‌ی **App-Specific Passwords** را انتخاب کنید.
4. یک رمز جدید با نام دلخواه (مثلاً `Sideloadly`) بسازید.
5. رمز تولیدشده را یادداشت کنید — در مرحله‌ی ۴٫۵ به آن نیاز دارید.

> ⚠️ در ابزار نصب، به‌جای رمز اصلی حساب خود، این رمز اختصاصی را وارد کنید.

---

## ۴. مراحل نصب

### ۴٫۱ — نصب ابزار Sideloadly

از نشانی [sideloadly.io](https://sideloadly.io) نسخه‌ی macOS را دانلود و نصب کنید.

### ۴٫۲ — اتصال دستگاه

آیفون را با کابل USB به مک متصل کنید. در صورت نمایش پیام **Trust This Computer?** روی صفحه‌ی گوشی، گزینه‌ی **Trust** را انتخاب و رمز عبور دستگاه را وارد کنید.

### ۴٫۳ — بارگذاری فایل

برنامه‌ی Sideloadly را اجرا کنید. دستگاه شما باید در بخش **Device** به‌صورت خودکار شناسایی شود. سپس فایل `mahem.ipa` را با ماوس به پنجره‌ی برنامه بکشید (Drag & Drop).

### ۴٫۴ — ورود اطلاعات حساب

نشانی ایمیل Apple ID خود را در فیلد **Apple Account** وارد کنید.

### ۴٫۵ — آغاز فرایند

روی دکمه‌ی **Start** کلیک کنید. در پنجره‌ی درخواست رمز عبور، **رمز اختصاصی** ساخته‌شده در بخش ۳ را وارد نمایید.

فرایند امضا و انتقال بسته به حجم فایل و سرعت اتصال، بین ۲ تا ۱۰ دقیقه زمان می‌برد. تا پایان کار کابل را جدا نکنید.

### ۴٫۶ — تأیید توسعه‌دهنده روی دستگاه

پس از اتمام نصب، آیکون برنامه روی صفحه‌ی گوشی ظاهر می‌شود، اما در نخستین اجرا با پیام خطای «Untrusted Developer» مواجه خواهید شد. برای رفع آن:

**Settings → General → VPN & Device Management**

نام حساب Apple ID خود را در فهرست انتخاب کرده و گزینه‌ی **Trust** را بزنید.

### ۴٫۷ — اجرا

اکنون برنامه قابل اجراست. ✅

---

## ۵. محدودیت‌های شناخته‌شده

این محدودیت‌ها ناشی از سیاست‌های اپل در مورد حساب‌های رایگان است و ارتباطی به عملکرد خودِ اپلیکیشن ندارد:

### ۵٫۱ — انقضای هفت‌روزه

اعتبار امضای برنامه پس از **۷ روز** به پایان می‌رسد و پس از آن برنامه باز نخواهد شد. برای ادامه‌ی استفاده، کافی است مراحل بخش ۴ را مجدداً تکرار کنید. اطلاعات و داده‌های درون برنامه در این فرایند حفظ می‌شود.

**راهکار جایگزین:** در صورت تمایل به تمدید خودکار، می‌توانید از ابزار **AltStore** ([altstore.io](https://altstore.io)) به‌جای Sideloadly استفاده کنید. این ابزار مؤلفه‌ای به نام `AltServer` روی مک نصب می‌کند که تا زمانی که مک و آیفون به یک شبکه‌ی Wi-Fi متصل باشند، امضا را به‌صورت خودکار و بدون نیاز به کابل تمدید می‌کند.

### ۵٫۲ — غیرفعال بودن اعلان‌ها (Push Notifications)

سرویس اعلان‌های اپل (APNs) روی حساب‌های رایگان در دسترس نیست. ابزار نصب این قابلیت را به‌صورت خودکار از برنامه حذف می‌کند تا نصب با موفقیت انجام شود.

در نتیجه، **هیچ اعلانی دریافت نخواهید کرد.** سایر بخش‌های برنامه به‌طور کامل و بدون اشکال کار می‌کنند. این مورد را لطفاً به‌عنوان ایراد گزارش نفرمایید.

### ۵٫۳ — سقف سه برنامه

با یک Apple ID رایگان، حداکثر **۳ برنامه** به‌صورت همزمان قابل نصب به این روش است.

---

## ۶. رفع اشکالات متداول

| پیام خطا / مشکل | علت | راه‌حل |
|---|---|---|
| `Untrusted Developer` | پروفایل تأیید نشده است | بخش ۴٫۶ را انجام دهید |
| خطای احراز هویت هنگام ورود | رمز اصلی وارد شده است | از رمز اختصاصی (بخش ۳) استفاده کنید |
| `Unable to find device` | دستگاه شناسایی نشده | کابل را بررسی کنید؛ گوشی باید قفل‌گشوده و Trust شده باشد |
| `Maximum App ID limit reached` | سقف ۱۰ شناسه در ۷ روز | تا پایان دوره صبر کنید یا برنامه‌های آزمایشی دیگر را حذف کنید |
| برنامه پس از چند روز باز نمی‌شود | انقضای امضا | بخش ۵٫۱ |
| توقف در مرحله‌ی `Installing` | حجم زیاد فایل | شکیبا باشید؛ تا ۱۰ دقیقه طبیعی است |

---

## ۷. تأیید صحت فایل

برای اطمینان از سالم بودن فایل دریافتی، می‌توانید در Terminal دستور زیر را اجرا کنید:

```bash
shasum -a 256 mahem.ipa
```

خروجی باید دقیقاً با مقدار درج‌شده در جدول ابتدای این سند مطابقت داشته باشد.

---

## ۸. ملاحظات

- این نسخه صرفاً جهت **ارزیابی و آزمون داخلی** ارائه شده است.
- لطفاً از بازتوزیع فایل خودداری فرمایید.
- گزارش اشکالات را همراه با **نسخه‌ی iOS**، **مدل دستگاه** و در صورت امکان **تصویر صفحه** ارسال کنید.

---

*این سند برای نسخه‌ی ۱٫۰ (بیلد ۱) تهیه شده است. برای بیلدهای بعدی، فایل و مقدار SHA-256 تغییر خواهد کرد.*
