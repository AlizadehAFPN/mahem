# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx512m -XX:MaxMetaspaceSize=256m
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
# Automatically convert third-party libraries to use AndroidX
android.enableJetifier=true

# Version of flipper SDK to use with React Native
FLIPPER_VERSION=0.182.0

# Use this property to specify which architecture you want to build.
# You can also override it from the CLI using
# ./gradlew <task> -PreactNativeArchitectures=x86_64
reactNativeArchitectures=armeabi-v7a,arm64-v8a,x86,x86_64

# Use this property to enable support to the new architecture.
# This will allow you to use TurboModules and the Fabric render in
# your application. You should enable this flag either if you want
# to write custom TurboModules/Fabric components OR use libraries that
# are providing them.
newArchEnabled=false

# Use this property to enable or disable the Hermes JS engine.
# If set to false, you will be using JSC instead.
hermesEnabled=true

# React Native 0.72 pins Android Gradle Plugin 7.4.2, which was tested only up to
# compileSdk 33 and hard-fails on anything newer. Google Play requires targeting
# API 35, so the check is suppressed rather than the target lowered.
android.suppressUnsupportedCompileSdk=35

# That same AGP 7.4.2 also ships an aapt2 that cannot read android-35.jar: API 35
# encodes its resource table sparsely and the old binary reports it as corruption
# ("RES_TABLE_TYPE_TYPE entry offsets overlap actual entry data"), which fails every
# resource-linking task. Pointing AGP at the aapt2 from build-tools 35.0.0 fixes it
# while leaving the rest of AGP alone. It becomes unnecessary once the project moves
# to AGP 8.6+, whose own aapt2 handles API 35.
#
# `android.aapt2FromMavenOverride` is the only knob AGP honours for it — overriding
# the aapt2 Maven coordinate via resolutionStrategy has no effect, because AGP
# resolves that binary outside normal dependency resolution. AGP accepts only an
# absolute path, so the value cannot be written here without hardcoding one
# machine's SDK location: it lived here as
# /Users/<a developer>/Library/Android/sdk/... and the Android build therefore
# could not be produced anywhere else.
#
# settings.gradle now derives it from this machine's SDK (local.properties sdk.dir,
# then ANDROID_HOME/ANDROID_SDK_ROOT, then the platform default location). Setting
# it explicitly here or with -Pandroid.aapt2FromMavenOverride=... still overrides
# that, which is the escape hatch for an SDK in a non-standard place.
