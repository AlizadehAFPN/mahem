<resources>

    <!-- Android 15 draws app content behind the status and navigation bars for
         every app that targets API 35+. React Native 0.72 does not apply window
         insets on its own, so letting that through would push the top of every
         screen under the status bar. Opting out keeps the pre-15 layout, which
         is what the screens were designed against.

         This escape hatch only works while targetSdk is 35 — Android 16 (API 36)
         ignores it. Before raising targetSdk to 36, the app has to handle insets
         itself (react-native-safe-area-context is already a dependency) and this
         file should go away. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
    </style>

</resources>
